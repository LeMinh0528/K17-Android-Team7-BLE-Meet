<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorblue100</item>
        <item name="colorPrimaryDark">@color/colorblue100</item>
        <item name="android:statusBarColor">@color/colorblue100</item>
        <item name="colorAccent">@color/colorblue100</item>
    </style>
    <style name="CustomChipChoice" parent="@style/Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/bg_chip_color</item>
        <item name="chipStrokeColor">@color/bg_chip_stroke</item>
        <item name="android:textColor">@color/colorgray400</item>
        <item name="chipStrokeWidth">2dp</item>
<!--        <item name="rippleColor">@color/colorblue</item>-->
    </style>
<!--    custom bottomsheetdialog-->
    <style name="BottomSheetTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="RoundedSquare">
        <item name="cornerSize">20%</item>
    </style>

</resources>